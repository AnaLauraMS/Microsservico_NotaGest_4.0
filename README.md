# 📦 Microserviço

## Docker

Este projeto utiliza Docker para facilitar a execução em qualquer ambiente.

---

## ⚙️ Comandos Docker

### 🔹 1. Build da imagem

```bash
docker build -t micronotagest .
````

### 🔹 2. Rodar o container

```bash
docker run -d -p 5001:5001 micronotagest
````
